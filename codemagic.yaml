workflows:
ios-build:
name: iOS Build
environment:
flutter: 3.22.0
scripts:
- flutter pub get
- rm -rf ios
- flutter create --platforms=ios .
- flutter build ios --release --no-codesign
artifacts:
- build/ios/ipa/*.ipa
