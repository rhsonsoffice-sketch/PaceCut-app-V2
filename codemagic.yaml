workflows:
ios-build:
name: iOS Build
environment:
flutter: 3.22.0
scripts:
- flutter pub get
- rm -rf ios # ✅ DELETE OLD BROKEN iOS FOLDER
- flutter create --platforms=ios . # ✅ GENERATE CLEAN, CORRECT iOS FILES
- flutter build ios --release --no-codesign
artifacts:
- build/ios/ipa/*.ipa
